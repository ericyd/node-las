/**
 * Provides method of removing piont data based on the options supplied
 * @param {object} options
 * @param {array} points
 * @return
 */

const filter = (options = {}, points = []) => {
  return points;
};

module.exports = filter;
